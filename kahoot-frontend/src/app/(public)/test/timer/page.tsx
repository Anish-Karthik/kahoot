import ReactCountDown from "@/app/(protected)/quiz/_components/ReactCountDown";
import React from "react";

const page = () => {
  return (
    <ReactCountDown className="fixed inset-0 z-50" />
  );
};

export default page;
